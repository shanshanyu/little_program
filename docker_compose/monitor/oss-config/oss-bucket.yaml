type: ALIYUNOSS
config:
  endpoint: "oss-cn-hangzhou-internal.aliyuncs.com"  # 替换为你的OSS endpoint
  bucket: "monitor-oss-yss"                # 替换为你的bucket名称
  # 替换为你的access key
  # 替换为你的secret key
  prefix: ""